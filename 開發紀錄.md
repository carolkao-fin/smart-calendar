# 排程曆 — 開發紀錄

## 專案資訊

| 項目 | 內容 |
|------|------|
| 專案名稱 | 排程曆（smart-calendar） |
| GitHub | https://github.com/carolkao-fin/smart-calendar |
| 部署平台 | GitHub Pages（主要）· Streamlit Cloud（次要） |
| 線上網址 | https://carolkao-fin.github.io/smart-calendar/ |
| 開發工具 | Claude Code (claude-opus-5) |
| 技術架構 | 原生 HTML／CSS／JavaScript 單檔（無外部相依） · Python／Streamlit（僅部署入口） |
| 開發日期 | 2026-08-01 |

---

## 功能說明

使用者輸入「什麼時候要完成」與「需要多久」，系統自動把工作切成小塊，
排進未來的工作日（預設週一至週五），並標示來不及的風險。

### 主要功能

- 任務新增／編輯／刪除，時間可用小時、天、週輸入
- 四種任務型態：一般任務、固定時段、無期限、純清單項目
- 每個任務可指定只在星期幾做，蓋過全域工作日
- 自動排程：跳過週末、休假日與非工作日，同一天會讓多件事各推進一點
- 月曆與工作週兩種檢視，日期格本身即為當日負載計
- 六種重複頻率：每天／每個工作日／每週／每兩週／每月／每年，完成後自動排到下一次
- 風險標示：從容／緊繃／會遲交／排不進／固定／已過／無期限
- 清單勾選框，完成即劃掉，並分「今天／接下來／無期限・清單／已完成」四段
- 已完成任務可一鍵恢復，還原成勾選前的進度與日期
- 完成紀錄頁：每日打卡與結案一覽，含今日／本週統計，每筆都可恢復
- 今日面板打卡記錄實際時數，剩餘部分自動重排
- 可調每日可投入時數、單一任務每日上限、每件事每天至少排、工作日、緩衝天數、排程策略
- 匯出 `.ics`（匯入 Google 日曆／Outlook）與 `.json` 備份還原

---

## 排程演算法

每一個工作日，從所有未完成任務中依「密度」排序，由高至低填滿當日可用時數：

```
密度 = 剩餘時數 ÷ 到期前還剩幾個工作天
```

越接近截止、剩越多的任務密度越高，越早被排入。填滿當日容量後換下一天，
週末、休假日、未勾選的工作日直接跳過。

**為何不用單純的最早截止優先（EDF）**：EDF 只看截止日，會讓一件三天後到期的
小事排在一件五天後到期的大事前面，即使後者根本做不完。密度同時反映了
「還剩多少」與「還有多少天」，能更早暴露真正的風險。

兩種策略：

| 策略 | 行為 |
|------|------|
| 平均分攤（預設） | 每件事排到某天就至少給 `每件事每天至少排`，密度更高才給更多（見挑戰 15） |
| 儘早做完 | 最急的任務直接吃滿當日容量上限 |

**排程順序**：固定時段先佔位（不受工作日與容量限制，並吃掉當日容量），
接著是有期限的彈性任務依密度排序，最後才輪到無期限任務撿剩餘容量。
純清單項目完全不進入排程。

**候選日**是「全域工作日 ∪ 各任務自訂星期」的聯集，
每個任務再從中篩出自己能用的日子；密度也以該任務自己的可用日計算剩餘天數。

---

## 開發歷程

### v1 初版（commit `049c47b`）

- `index.html`：單檔純前端行事曆，含排程演算法、月曆／工作週檢視、
  設定面板、`.ics` 與 JSON 匯出
- `line_bot.py`：LINE webhook、網頁同步 API、每日推播，排程邏輯與網頁端一致
- `README.md`：使用說明與 LINE Bot 架設步驟

### v2 修正 meta 標籤（commit `d53e8ae`）

- 補上 `<meta charset>` 與 viewport

### v3 修正授權漏洞（commit `55ca81b`）

- LINE Bot 加入擁有者驗證，未授權者一律擋下
- 同步 API 改為 fail-closed
- README 補上個資流向說明

### v4 時間單位（commit `5b80356`）

- 新增與編輯表單加入小時／天／週單位選單，並即時顯示換算結果
- LINE 端同步支援 `2天`／`1週`／`3工作天`

### v5 四種任務型態與每任務工作日（commit `32afb43`）

- 截止日改為選填：無期限任務撿剩餘容量，不會被標成「會遲交」
- 未填時數＝純清單項目，不進行事曆
- 選填特定時間＝固定時段，不拆塊並優先佔用當日容量
- 每個任務可指定只在星期幾做，蓋過全域工作日設定
- 任務清單加入勾選框與「排程中／清單・無期限／已完成」分組
- 修正每日上限在趕不上截止日時造成的假警報（見下方挑戰 9）

### v6 移除 LINE 連動（commit `b1d5ad1`）

- 刪除 `line_bot.py`、網頁端同步面板與定時同步邏輯、相關相依套件與說明
- 行事曆功能完全不受影響；移除後不再有任何後端，資料只存在瀏覽器
- 新增 `test_browser.py`：以 headless Chrome 實際執行頁面並驗證渲染結果
  （見挑戰 10 —— 這是移除 LINE 後才發現一直做得到的事）

### v7 重複頻率、恢復與完成紀錄

- 新增與編輯表單加入「重複頻率」，六種頻率；完成後自動滾到下一次，
  時數歸零、其他設定保留，日曆上以虛線框預告之後的次數
- 任務清單分組改為「今天／接下來／無期限・清單／已完成」，
  今天這段同時收今日到期、已經逾期、以及今天有排到工作的事
- 已完成任務加上「恢復」鍵；恢復是還原快照，不再把進度歸零
- 新增「完成紀錄」檢視：每日打卡與結案一覽，含今日／本週統計，每筆都能恢復
- `test_browser.py` 從只讀畫面擴充為實際操作：點勾選框、打卡、按恢復，
  再從渲染結果驗證（34 項檢查）

### v8 平均分攤改成真的每天都推進

- 平均分攤模式加入「保底」：當天每件能做的事先各拿 `MIN_SLICE`（0.5h），
  剩餘容量再照密度分配；保底最多只用掉當天一半的容量
- 同一件事當天分兩次拿到的時數合併成一塊，日曆上不會出現重複的區塊
- 每日上限改為以「當天已拿到的總量」計算，保底不會架空上限
- 設定裡的策略名稱改為「平均分攤（每天都推進）」，說明文字一併改寫

### v9 保底改成可調，預設 2 小時

- v8 的 0.5h 保底被回報「切太碎」，改為新設定「每件事每天至少排」，預設 2 小時
- 兩輪分配收回成一輪：`想要的時數 = max(每件至少排, 密度需求)`，
  照密度順序取用當日容量，取完就換下一天
- 每日上限改為 `max(單一任務每日上限, 密度需求, 每件至少排)`，
  免得使用者把保底調到比上限大時被默默壓回去
- 設定畫面即時顯示「以目前的組合，一天大約排幾件」

### v10 今日面板就地恢復

- 打卡完的項目改為留在今日面板上（劃掉、附「恢復」），不再直接消失
- 面板加上「今天完成」小標與當日累計時數
- 當天排定的都做完時，空狀態文案從「今天沒有排定的工作」改為「今天排定的都做完了」

理由：打卡是最容易手滑的操作，而原本按錯之後畫面上不留任何痕跡 ——
那一列直接消失，使用者得先想到有「完成紀錄」這一頁、再切過去才找得回來。
恢復鍵應該出現在犯錯的地方。

---

## 技術挑戰與解法

### 1. 兩份排程演算法必須保持一致

> 此功能已於 v6 移除，以下為當時的紀錄。

**問題**：網頁端（JavaScript）與 LINE Bot（Python）各需要一份排程實作。
若兩邊行為不同，同一份任務在網頁和 LINE 會看到不一樣的安排。

**解法**：兩邊採用完全相同的演算法結構與變數命名，並在 README 與
兩份原始碼中明確註記「改動排程規則時兩邊都要改」。

**工作日編號的坑**：JavaScript `Date.getDay()` 是 0=週日～6=週六，
Python `date.isoweekday()` 是 1=週一～7=週日。設定會在兩端之間同步，
若各用各的編號，使用者在網頁勾選「週日」存成 `0`，Python 端永遠不會匹配。
最後統一採 JavaScript 慣例，Python 端以 `d.isoweekday() % 7` 轉換。

### 2. 缺少 charset 與 viewport

**問題**：`index.html` 最初是為內嵌環境撰寫的，未加 `<meta charset>` 與 viewport。
GitHub Pages 有 HTTP header 可依靠所以正常，但以 `file://` 在本機直接開啟時
沒有 header，中文變成亂碼 —— 而當時的說明正是建議把檔案存到本機開啟。
手機上則因缺 viewport 而以桌面寬度渲染後整頁縮小。

**解法（commit `d53e8ae`）**：補上 `charset`、`viewport`、`color-scheme` 三個 meta。

### 3. LINE Bot 任何人都能讀寫全部任務

> 此功能已於 v6 移除，以下為當時的紀錄。

**問題**：`handle_text()` 對單一全域 store 操作，webhook 收到事件後
完全沒有比對發話者身分。LINE 官方帳號任何人都加得到，因此陌生人可以：
打「清單」看光所有任務與截止日、打「刪除 X」直接刪除任務。
更嚴重的是 `push_loop` 會把完整任務清單推播給 `store["users"]` 中的每一個人。

**解法（commit `55ca81b`）**：
- 新增 `is_authorised()`，只服務擁有者，其餘一律回覆制式訊息並記錄日誌
- 擁有者由 `ALLOWED_USER_IDS` 環境變數指定；未指定時採「先到先得」，
  第一個與 Bot 對話的人成為擁有者，並在日誌印出其 user ID 供填入設定
- 每日推播對象限縮為擁有者，不再使用 `store["users"]`

### 4. 同步 API 為 fail-open

> 此功能已於 v6 移除，以下為當時的紀錄。

**問題**：原本的權杖檢查是

```python
if SYNC_TOKEN and request.headers.get("X-Sync-Token", "") != SYNC_TOKEN:
    abort(401)
```

`SYNC_TOKEN` 環境變數未設定時是空字串，整個條件短路跳過，
同步 API 完全不設防 —— 任何知道網址的人都能讀取或覆寫全部任務。
「忘了設定就全開」是危險的預設值。

**解法（commit `55ca81b`）**：
- 未設定密鑰時直接停用同步 API（回 503），而非放行
- 比對改用 `hmac.compare_digest()` 避免時序側錄
- 先轉為 bytes 再比對，支援含中文的密鑰（`compare_digest` 收非 ASCII str 會丟 `TypeError`）

### 5. 「2 天」到底是幾小時

**問題**：以工作量估時，「這份報告要兩天」比「要八小時」自然，
但「2 天」有歧義 —— 是兩個工作天的可投入時間，還是連續 48 小時？

**解法（commit `5b80356`）**：
- 換算基準取自使用者自己的設定：`1 天 = 每日可投入時數`、
  `1 週 = 工作日數 × 每日可投入時數`；改設定時換算跟著變
- 輸入框下方**即時顯示換算後的小時數**，送出前就看得到實際會排多少
- 資料一律以小時存放，單位僅為輸入時的便利，不往下游擴散
  （否則日曆上會出現「0.125 天」這種讀不出意義的數字）

**兩個表單的換單位行為刻意不同**：新增表單換單位是「改變心意」
（打了 3 再選「天」＝ 3 天）；編輯既有任務換單位是「換個說法看同一件事」，
8h 切到「天」會顯示 2，數值真的換算過去。

### 6. 「日」與日期寫法相撞

> 此功能已於 v6 移除，以下為當時的紀錄。

**問題**：LINE 端支援 `2天`、`1週` 後，若一併收「日」作為單位，
「20日」會被誤讀成 20 天 = 80 小時。

**解法（commit `5b80356`）**：時間單位刻意只收
`天／工作天／週／周／禮拜／星期`，不收「日」。
並加入測試確認 `20日`、`2026`、`8/20`、`第三章` 都不會被誤認為時間。

### 7. localStorage 在 iframe 內可能失效

**問題**：Streamlit Cloud 是把網頁放進 iframe 呈現。
若瀏覽器封鎖第三方網站資料，`localStorage` 會丟例外，
原本的 `try/catch` 只是靜靜吞掉 —— 使用者操作一切正常，關掉分頁資料就消失。

**解法**：啟動時先做一次探測性寫入，失敗則在頁面上方顯示橘色警示，
告知資料不會保存並建議改用匯出備份或 GitHub Pages 網址。

### 8. 開發環境沒有 Node 或瀏覽器

**問題**：開發機只有 Python，無法執行 JavaScript 或渲染網頁來驗證前端。

**當時的解法**：
- 撰寫 Python 腳本對內嵌 JS 做括號平衡檢查（正確跳過字串、樣板字面值、
  正規表達式與註解），並交叉比對所有 `$("#id")` 引用與 HTML 中的 `id` 屬性
- 排程演算法另有一份 Python 實作（LINE Bot 用），因此以 Python 端撰寫測試
  間接驗證邏輯：時數守恆、不排週末、每日不超載、休假日避開、超載正確標為風險
- Streamlit 入口以 headless 模式實際啟動並探測健康端點

**後來的發現（v6）**：移除 LINE 時才想到去找瀏覽器，結果開發機一直裝著 Chrome。
`--headless --dump-dom` 就能真的把頁面跑起來，這個限制從 v1 起其實一直不成立。
見挑戰 10。

### 9. 每日上限造成「明明有容量卻說會遲交」

**問題**：加入四種任務型態後測試才發現的舊缺陷。
一件 6 小時、兩個工作天到期的任務被判定「會遲交」，
但那兩天其實有 8 小時容量 —— 卡住它的是「單一任務每日上限 2.5h」。
工具一邊宣告你會遲交、一邊留著容量不用，等於給出假警報。

**解法（commit `32afb43`）**：每日上限只在來得及的時候生效。
若任務所需的每日時數（密度）已經超過上限，就以密度取代上限：

```
單任務當日可取用 = max(每日上限, 該任務的密度)
```

無期限任務不適用此放寬 —— 它們本來就沒有非趕不可的理由，
上限照舊，免得反過來把有期限的任務擠掉。

### 10. 一直以為沒有辦法測前端

**問題**：v1 到 v5 的「已知限制」都寫著「前端未經瀏覽器實測」，
因為開發機沒有 Node。移除 LINE 時，Python 版排程實作跟著消失，
等於連間接驗證演算法的手段都沒了 —— 這才促使我回頭檢查這個前提。

**解法（v6）**：開發機其實裝著 Chrome。
`chrome --headless=new --dump-dom` 會執行頁面上的 JavaScript 並印出渲染後的 DOM，
於是可以注入一段探針把畫面狀態塞進 `document.title` 帶回來，
再用 Python 解析、斷言。`test_browser.py` 就是這樣寫的，
驗證的是**瀏覽器實際算出來、實際畫出來**的結果，
而不是重寫一份邏輯來對答案 —— 比先前的 Python 間接驗證更可靠。

教訓：「做不到」的前提值得定期重新檢查，尤其是它已經寫進文件、
開始被當成事實引用的時候。

### 11. 自訂星期必須能突破全域工作日

**問題**：「這件事只在六日做」的前提是全域工作日設為週一至週五，
若把每個任務的可用日與全域工作日取交集，六日永遠是空集合，任務排不進去。

**解法（commit `32afb43`）**：候選日改為
「全域工作日 ∪ 所有任務自訂的星期」的聯集，
每個任務再從中篩出自己能用的日子。密度計算也改用**該任務自己的**
可用日清單來數「還剩幾天」，否則只在週六做的任務會誤以為每天都能做。

### 12. 「恢復」不能只是把 `done` 設回 `false`

**問題**：v5 加勾選框時，取消勾選的處理是

```js
t.done = false;
if((t.doneHours || 0) >= t.hours) t.doneHours = 0;
```

因為標記完成時把 `doneHours` 直接寫成 `hours`，取消時只好整個歸零 ——
一件做到 8/10 的事，誤按一下再取消，進度就變成 0/10，而且救不回來。
使用者最常按錯的那個鍵，代價卻是資料損失。

**解法（v7）**：改成**每次異動都存快照**。打卡與標記完成都會寫一筆紀錄，
附上異動前的 `deadline / start / doneHours / done`；恢復就是把快照寫回去、
刪掉那筆紀錄。所以恢復能還原的不只是「未完成」這個狀態，
還包括原本做到幾小時、重複任務原本是哪一次。
取消勾選也走同一條路徑：先找最後一筆完成紀錄來還原，
真的沒有紀錄（v6 以前的舊資料）才退回舊行為。

### 13. 重複任務要不要預先排進未來？

**問題**：「每週一小時的進度回報」如果把未來每一次都展開成任務排進日曆，
看起來最完整，但會壞掉三件事：每日負載被還沒發生的事填滿、
風險判斷跟著失真（未來的重複次數會把真正有截止日的事擠成「排不進」），
而且沒有結束日的重複要展開到哪一天永遠沒有答案。

**解法（v7）**：**只排「這一次」**。排程演算法完全不知道重複的存在，
一件重複任務在任何時刻都只是一件普通任務；完成時才滾到下一次。
之後的次數只在日曆上以虛線框預告，不佔時數、不進 `plan`、不影響風險判斷。
預告的範圍也只算到目前畫面最後一格為止，換月才往後長。

代價是 `.ics` 匯出只含這一次 —— 匯出檔本來就是「現在排好的樣子」的快照，
重排後要重新匯出，這一點與既有行為一致。

### 14. 錯過的重複次數不該往前補

**問題**：每週一要交的東西，拖到隔週三才按完成。若單純把截止日 `+7`，
新的截止日還是過去式，下次再完成又 `+7`，永遠追不上今天，
畫面上會一直掛著逾期的紅字。

**解法（v7）**：`nextOccurAfter()` 一路往後推到**第一個大於今天**的日子才停，
中間錯過的幾次直接跳過（那些日子已經過了，補上去沒有意義）。
迴圈另設 400 次上限，避免資料異常時卡死。

同時處理兩個日期細節：

- **每月要夾住月底**：1/31 的下一次是 2/28 而不是 3/3，
  `addMonths()` 先算出目標月份的最後一天再取 `min`
- **「每個工作日」跟著設定走**：使用者把工作日全部取消時，
  「找下一個工作日」的迴圈會永遠找不到，此時退回預設的週一至週五

### 15. 「平均分攤」其實沒有在分攤

**問題**：使用者的回報是「希望每一天每一件事都推進一點，而不是集中在前幾天完成」。
名字叫平均分攤，行為卻不是：每天照密度由高到低填滿容量，
密度最高的那件先拿走 `upHalf(密度)`，容量常常在輪到第三件之前就用完。
以預設的 4h 容量、三件任務為例，密度最低的那件連續好幾天都分不到任何時數 ——
從使用者的角度看，就是「這件事被冷凍了」。

分攤的其實是**時間**（每件事每天只拿一小塊，不會一次做完），
但不是**注意力**（今天到底碰得到幾件事）。使用者要的是後者。

**第一次的解法（v8，已被取代）**：每天分兩輪。第一輪保底，
當天每件能做的事各拿 `MIN_SLICE`（0.5h）；第二輪再照密度分配剩餘容量，
保底最多只用掉當天一半容量，免得十件事把一天切成十個 0.5h。

**使用者的回饋**：「有點切太碎，2～3 小時差不多。」
方向對了，粒度錯了 —— 0.5h 的區塊排出來根本做不動，
「每件事都推進一點」被實作成「每件事都推進一點點」。
真正該調的不是輪數，是**一塊有多大**。

**現在的解法（v9）**：保底改成使用者可調的設定「每件事每天至少排」（預設 2h），
兩輪收回成一輪：

```
想要的時數 = max(每件事每天至少排, 密度需求)
```

照密度順序取用當日容量，取完就換下一天。一天排得下幾件，
自然就是 `每日可投入時數 ÷ 每件事每天至少排`（預設 4 ÷ 2 = 2 件），
這個算式直接顯示在設定畫面上，使用者不必自己推。

兩個順帶的修正：
每日上限改為 `max(單一任務每日上限, 密度需求, 每件至少排)` ——
使用者把保底調到比上限大時，不該被默默壓回上限；
一件事一天只取一次，`plan` 裡不會再出現同一天同名的兩塊。

**代價要講清楚**：容量被分掉，最急的任務就會晚一點完成。
以內建範例來說，一天 4h 配 2h 保底，等於一天只碰兩件事 ——
想一天碰到更多件，就得把保底調小或把每日可投入時數調大。
這個取捨現在握在使用者手上，而不是寫死在程式裡。

---

## 部署架構

兩個部署目標：

| 平台 | 放什麼 | 為什麼 |
|------|--------|--------|
| GitHub Pages | `index.html` | 純前端網頁的正確歸宿：載入快、網址固定、localStorage 正常 |
| Streamlit Cloud | `streamlit_app.py` 內嵌 `index.html` | 僅為方便，實質是 iframe 包裝 |

曾經還有第三個目標 —— LINE Bot 需要能接收 POST 的常駐伺服器（Render／Railway），
因為 GitHub Pages 只能提供靜態檔案、Streamlit 不能自訂 HTTP 路由。該功能已於 v6 移除。

---

## 個資與隱私

移除 LINE 連動後，這個工具不再蒐集或傳輸任何個人資料：
沒有後端、沒有帳號、沒有追蹤，也不載入任何外部字型或分析工具。
任務資料只存在使用者自己裝置的瀏覽器 localStorage 裡。

v1～v5 期間的 LINE 連動會把任務名稱送到 LINE 伺服器，
並在自架主機上以明文保存任務與 LINE user ID；該路徑已隨功能移除而消失。

---

## 已知限制

| 限制 | 說明 |
|------|------|
| 無多裝置同步 | 資料存在各裝置自己的瀏覽器，跨裝置需自行匯出匯入 JSON |
| Streamlit 版可能無法保存 | iframe 內 localStorage 可能被瀏覽器封鎖，此時頁面會顯示警示 |
| 測試依賴範例資料 | `test_browser.py` 驗證的是 `index.html` 內建的 seed，改動 seed 時測試也要跟著改 |
| `.ics` 只含這一次 | 重複任務不展開成未來的日曆事件，理由見挑戰 13 |
| 完成紀錄只留 600 筆 | 超過就從最舊的丟掉，避免 localStorage 無限長大 |
| 重複沒有結束日 | 目前無法指定「重複到某天為止」，不需要了就自己改成「不重複」 |

---

## 相依套件

```
streamlit>=1.32
```

網頁端（`index.html`）無任何外部相依，不載入 CDN、字型或分析工具。

---

## 檔案結構

```
index.html        網頁本體，單檔、無外部相依
streamlit_app.py  Streamlit Cloud 入口，內嵌 index.html
test_browser.py   以 headless Chrome 實際跑頁面並驗證渲染結果
requirements.txt  相依套件
README.md         使用說明
開發紀錄.md       本文件
```
