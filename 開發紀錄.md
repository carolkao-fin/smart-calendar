# 排程曆 — 開發紀錄

## 專案資訊

| 項目 | 內容 |
|------|------|
| 專案名稱 | 排程曆（smart-calendar） |
| GitHub | https://github.com/carolkao-fin/smart-calendar |
| 部署平台 | GitHub Pages（主要）· Streamlit Cloud（次要） |
| 線上網址 | https://carolkao-fin.github.io/smart-calendar/ |
| 開發工具 | Claude Code (claude-opus-5) |
| 技術架構 | 原生 HTML／CSS／JavaScript 單檔 · Python · Flask · LINE Messaging API |
| 開發日期 | 2026-08-01 |

---

## 功能說明

使用者輸入「什麼時候要完成」與「需要多久」，系統自動把工作切成小塊，
排進未來的工作日（預設週一至週五），並標示來不及的風險。

### 主要功能

- 任務新增／編輯／刪除，時間可用小時、天、週輸入
- 自動排程：跳過週末、休假日與非工作日
- 月曆與工作週兩種檢視，日期格本身即為當日負載計
- 風險標示：從容／緊繃／會遲交／排不進
- 今日面板打卡記錄實際時數，剩餘部分自動重排
- 可調每日可投入時數、單一任務每日上限、工作日、緩衝天數、排程策略
- 匯出 `.ics`（匯入 Google 日曆／Outlook）與 `.json` 備份還原
- LINE Bot 連動（選用）：對話新增任務、每日推播、與網頁雙向同步

---

## 排程演算法

每一個工作日，從所有未完成任務中依「密度」排序，由高至低填滿當日可用時數：

```
密度 = 剩餘時數 ÷ 到期前還剩幾個工作天
```

越接近截止、剩越多的任務密度越高，越早被排入。填滿當日容量後換下一天，
週末、休假日、未勾選的工作日直接跳過。

**為何不用單純的最早截止優先（EDF）**：EDF 只看截止日，會讓一件三天後到期的
小事排在一件五天後到期的大事前面，即使後者根本做不完。密度同時反映了
「還剩多少」與「還有多少天」，能更早暴露真正的風險。

兩種策略：

| 策略 | 行為 |
|------|------|
| 平均分攤（預設） | 每個任務每天只給 `密度` 換算後的一小塊，容量用不完就留著 |
| 儘早做完 | 最急的任務直接吃滿當日容量上限 |

---

## 開發歷程

### v1 初版（commit `049c47b`）

- `index.html`：單檔純前端行事曆，含排程演算法、月曆／工作週檢視、
  設定面板、`.ics` 與 JSON 匯出
- `line_bot.py`：LINE webhook、網頁同步 API、每日推播，排程邏輯與網頁端一致
- `README.md`：使用說明與 LINE Bot 架設步驟

### v2 修正 meta 標籤（commit `d53e8ae`）

- 補上 `<meta charset>` 與 viewport

### v3 修正授權漏洞（commit `55ca81b`）

- LINE Bot 加入擁有者驗證，未授權者一律擋下
- 同步 API 改為 fail-closed
- README 補上個資流向說明

### v4 時間單位（commit `5b80356`）

- 新增與編輯表單加入小時／天／週單位選單，並即時顯示換算結果
- LINE 端同步支援 `2天`／`1週`／`3工作天`

---

## 技術挑戰與解法

### 1. 兩份排程演算法必須保持一致

**問題**：網頁端（JavaScript）與 LINE Bot（Python）各需要一份排程實作。
若兩邊行為不同，同一份任務在網頁和 LINE 會看到不一樣的安排。

**解法**：兩邊採用完全相同的演算法結構與變數命名，並在 README 與
兩份原始碼中明確註記「改動排程規則時兩邊都要改」。

**工作日編號的坑**：JavaScript `Date.getDay()` 是 0=週日～6=週六，
Python `date.isoweekday()` 是 1=週一～7=週日。設定會在兩端之間同步，
若各用各的編號，使用者在網頁勾選「週日」存成 `0`，Python 端永遠不會匹配。
最後統一採 JavaScript 慣例，Python 端以 `d.isoweekday() % 7` 轉換。

### 2. 缺少 charset 與 viewport

**問題**：`index.html` 最初是為內嵌環境撰寫的，未加 `<meta charset>` 與 viewport。
GitHub Pages 有 HTTP header 可依靠所以正常，但以 `file://` 在本機直接開啟時
沒有 header，中文變成亂碼 —— 而本機開啟正是使用 LINE 同步功能的建議方式。
手機上則因缺 viewport 而以桌面寬度渲染後整頁縮小。

**解法（commit `d53e8ae`）**：補上 `charset`、`viewport`、`color-scheme` 三個 meta。

### 3. LINE Bot 任何人都能讀寫全部任務

**問題**：`handle_text()` 對單一全域 store 操作，webhook 收到事件後
完全沒有比對發話者身分。LINE 官方帳號任何人都加得到，因此陌生人可以：
打「清單」看光所有任務與截止日、打「刪除 X」直接刪除任務。
更嚴重的是 `push_loop` 會把完整任務清單推播給 `store["users"]` 中的每一個人。

**解法（commit `55ca81b`）**：
- 新增 `is_authorised()`，只服務擁有者，其餘一律回覆制式訊息並記錄日誌
- 擁有者由 `ALLOWED_USER_IDS` 環境變數指定；未指定時採「先到先得」，
  第一個與 Bot 對話的人成為擁有者，並在日誌印出其 user ID 供填入設定
- 每日推播對象限縮為擁有者，不再使用 `store["users"]`

### 4. 同步 API 為 fail-open

**問題**：原本的權杖檢查是

```python
if SYNC_TOKEN and request.headers.get("X-Sync-Token", "") != SYNC_TOKEN:
    abort(401)
```

`SYNC_TOKEN` 環境變數未設定時是空字串，整個條件短路跳過，
同步 API 完全不設防 —— 任何知道網址的人都能讀取或覆寫全部任務。
「忘了設定就全開」是危險的預設值。

**解法（commit `55ca81b`）**：
- 未設定密鑰時直接停用同步 API（回 503），而非放行
- 比對改用 `hmac.compare_digest()` 避免時序側錄
- 先轉為 bytes 再比對，支援含中文的密鑰（`compare_digest` 收非 ASCII str 會丟 `TypeError`）

### 5. 「2 天」到底是幾小時

**問題**：以工作量估時，「這份報告要兩天」比「要八小時」自然，
但「2 天」有歧義 —— 是兩個工作天的可投入時間，還是連續 48 小時？

**解法（commit `5b80356`）**：
- 換算基準取自使用者自己的設定：`1 天 = 每日可投入時數`、
  `1 週 = 工作日數 × 每日可投入時數`；改設定時換算跟著變
- 輸入框下方**即時顯示換算後的小時數**，送出前就看得到實際會排多少
- 資料一律以小時存放，單位僅為輸入時的便利，不往下游擴散
  （否則日曆上會出現「0.125 天」這種讀不出意義的數字）

**兩個表單的換單位行為刻意不同**：新增表單換單位是「改變心意」
（打了 3 再選「天」＝ 3 天）；編輯既有任務換單位是「換個說法看同一件事」，
8h 切到「天」會顯示 2，數值真的換算過去。

### 6. 「日」與日期寫法相撞

**問題**：LINE 端支援 `2天`、`1週` 後，若一併收「日」作為單位，
「20日」會被誤讀成 20 天 = 80 小時。

**解法（commit `5b80356`）**：時間單位刻意只收
`天／工作天／週／周／禮拜／星期`，不收「日」。
並加入測試確認 `20日`、`2026`、`8/20`、`第三章` 都不會被誤認為時間。

### 7. localStorage 在 iframe 內可能失效

**問題**：Streamlit Cloud 是把網頁放進 iframe 呈現。
若瀏覽器封鎖第三方網站資料，`localStorage` 會丟例外，
原本的 `try/catch` 只是靜靜吞掉 —— 使用者操作一切正常，關掉分頁資料就消失。

**解法**：啟動時先做一次探測性寫入，失敗則在頁面上方顯示橘色警示，
告知資料不會保存並建議改用匯出備份或 GitHub Pages 網址。

### 8. 開發環境沒有 Node 或瀏覽器

**問題**：開發機只有 Python，無法執行 JavaScript 或渲染網頁來驗證前端。

**解法**：
- 撰寫 Python 腳本對內嵌 JS 做括號平衡檢查（正確跳過字串、樣板字面值、
  正規表達式與註解），並交叉比對所有 `$("#id")` 引用與 HTML 中的 `id` 屬性，
  確認無遺漏也無多餘
- 排程演算法本來就有 Python 版本，因此以 Python 端撰寫測試間接驗證邏輯：
  時數守恆、不排週末、每日不超載、休假日避開、超載正確標為風險
- Streamlit 入口以 headless 模式實際啟動並探測健康端點

---

## 部署架構

三個部署目標各有能與不能：

| 平台 | 放什麼 | 為什麼 |
|------|--------|--------|
| GitHub Pages | `index.html` | 純前端網頁的正確歸宿：載入快、網址固定、localStorage 正常 |
| Streamlit Cloud | `streamlit_app.py` 內嵌 `index.html` | 僅為方便，實質是 iframe 包裝 |
| Render／Railway | `line_bot.py` | LINE webhook 需要能接收 POST 的常駐伺服器 |

**LINE Bot 為何不能放在前兩者**：GitHub Pages 只能提供靜態檔案，
無法接收 webhook；Streamlit 不能自訂 HTTP 路由，同樣無法提供 `/callback` 端點。

---

## 個資與隱私

僅使用網頁時，資料完全不離開瀏覽器。接上 LINE 後：

| 資料 | 存放位置 | 可見範圍 |
|------|----------|----------|
| 對話訊息（含任務名稱） | LINE 伺服器 | LINE 公司，依其隱私權政策 |
| 任務名稱、截止日、時數 | 伺服器 `store.json`，明文 | 能存取該主機者 |
| LINE user ID | 同上 | 同上 |

`store.json` 已列入 `.gitignore`。敏感事項建議在 LINE 中以代號表示，
細節留在網頁端補充。

---

## 已知限制

| 限制 | 說明 |
|------|------|
| 排程邏輯雙份維護 | `index.html` 與 `line_bot.py` 各有一份 `schedule()`，修改時兩邊都要改 |
| 無多裝置同步 | 網頁資料存在各自瀏覽器；跨裝置需自行匯出匯入，或接上 LINE Bot 伺服器 |
| LINE Bot 需另行部署 | GitHub Pages 與 Streamlit Cloud 皆無法接收 webhook |
| Streamlit 版可能無法保存 | iframe 內 localStorage 可能被瀏覽器封鎖，此時頁面會顯示警示 |
| 單人使用 | Bot 只服務一位擁有者，未設計多使用者的資料隔離 |
| 前端未經瀏覽器實測 | 開發環境無 Node 與瀏覽器，UI 以結構檢查與演算法測試間接驗證 |

---

## 相依套件

```
streamlit>=1.32
flask>=3.0
requests>=2.31
```

網頁端（`index.html`）無任何外部相依，不載入 CDN、字型或分析工具。

---

## 檔案結構

```
index.html        網頁本體，單檔、無外部相依
streamlit_app.py  Streamlit Cloud 入口，內嵌 index.html
line_bot.py       LINE webhook + 同步 API + 每日推播
requirements.txt  相依套件
README.md         使用說明與架設步驟
開發紀錄.md       本文件
store.json        LINE Bot 本機資料（執行後自動產生，已列入 .gitignore）
```
